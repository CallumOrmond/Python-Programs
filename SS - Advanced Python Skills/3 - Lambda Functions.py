#unnamed Simple methods - Functions 
#for Functional programs 

#Syntax --> lambda arguments : expression

number = 10 
add = lambda x: x + number
print(add(7))

muti = lambda x, y: x * y
print(muti(10,15))

#NOTES 
#   -Can take any number of params 
#   -Hard to track errors - care 
#   -Can use variables
#   -Created at runtime
